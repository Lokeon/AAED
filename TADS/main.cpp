#include <iostream>
#include "TadConsultorio.hpp"
#include "TadSimbolo.hpp"

int main(void)
{
    //TAD CONSULTORIO
    // Consultorio consult;
    // Medico uno("Pepe", 1);
    // Medico dos("Cepeda Calvo", 2);
    // Paciente pa("Enfermo1", 1);
    // Paciente pas("Enfermo2", 2);

    // consult.altaMedico(uno);
    // consult.altaMedico(dos);
    // consult.esperaPaciente(pa, uno);

    // consult.consultaMedico(dos);

    //TAD SIMBOLO
    Simbolo sim, x, y, xy;

    sim
}